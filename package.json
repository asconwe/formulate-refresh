{
  "name": "formulate refresh",
  "version": "1.0.0",
  "description": "Formulate is a streamlined form-builder and response-manager, designed to make the process of sharing surveys or soliciting applications a breeze for everyone involved. Organizations can easily build custom forms and view, manage, and analyze responses, while, on the other end, users just fill out and click submit. Gone are the days of the emailed PDF that the user forgot to save before they sent it back.",
  "main": "server.js",
  "scripts": {
    "test": "mocha --timeout 10000 --recursive",
    "endpoint-tests": "mocha --timeout 10000 test/endpoint-tests/*.test.js",
    "unit-tests": "mocha test/unit-tests/*.test.js",
    "single-test": "mocha test/single-test/*.test.js",
    "start": "SET NODE_ENV=development && node server.js",
    "restart": "SET NODE_ENV=development && nodemon server.js"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/asconwe/formulate-refresh.git"
  },
  "author": "August Conwell",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/asconwe/formulate-refresh/issues"
  },
  "homepage": "https://github.com/asconwe/formulate-refresh#readme",
  "dependencies": {
    "bcrypt": "^1.0.2",
    "bluebird": "^3.5.0",
    "body-parser": "^1.17.2",
    "cookie-parser": "^1.4.3",
    "cookie-session": "^2.0.0-beta.2",
    "dotenv": "^4.0.0",
    "express": "^4.15.3",
    "express-session": "^1.15.3",
    "mongoose": "^4.11.0",
    "morgan": "^1.8.2",
    "nodemailer": "^4.0.1",
    "passport": "^0.3.2",
    "passport-local": "^1.0.0"
  },
  "devDependencies": {
    "chai": "^4.1.1",
    "chai-http": "^3.0.0",
    "mocha": "^3.5.0"
  }
}
